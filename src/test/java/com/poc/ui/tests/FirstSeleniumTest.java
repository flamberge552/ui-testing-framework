package com.poc.ui.tests;

import com.poc.ui.utils.ReflectUtils;
import org.testng.Assert;
import org.testng.annotations.*;

public class FirstSeleniumTest extends BaseTest {
    @Test
    public void canGoToLoginPage1() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage2() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage3() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage4() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage5() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage7() {
        getDriver().get("https://www.google.com");

//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage8() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage9() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage10() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage11() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage12() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage13() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage14() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage15() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage16() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage17() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage18() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage19() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage20() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage21() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage22() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage23() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }

    @Test
    public void canGoToLoginPage34() {
        getDriver().get("https://www.google.com");
//
//        try {
//            Thread.sleep(4000);
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }

        Assert.assertEquals(getDriver().getTitle(), "Google", "Expected: 'Google', but got: " + getDriver().getTitle());
    }
}
